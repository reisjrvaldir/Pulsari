# Imagens reais pendentes

Pasta reservada para os assets reais da Pulsari que ainda não existem neste projeto:

- `logo.svg` — logotipo oficial (ver `src/components/Logo.tsx`)
- fotos dos fundadores (ver `src/components/About.tsx`)
- imagens/screenshots reais dos projetos do portfólio (ver `src/components/Portfolio.tsx`)

Nenhuma imagem foi inventada — ver `README.md` na raiz do projeto para a lista completa de pendências.
